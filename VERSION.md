# Plekxa Enterprise OS v1.9.1

Hotfix for the v1.9 invitation acceptance page:

- Added explicit Supabase `AuthChangeEvent` and `Session` TypeScript types.
- Resolves the Vercel build error: `Parameter '_event' implicitly has an 'any' type`.
- Retains all v1.9 invitation and Analytics report functionality.
