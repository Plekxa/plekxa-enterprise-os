import {createClient} from '@supabase/supabase-js';
export const dynamic='force-dynamic';
export default async function Page(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;const s=u&&k?createClient(u,k,{auth:{persistSession:false}}):null;const {data}=s?await s.from('index_certificates').select('*').order('created_at',{ascending:false}).limit(250):{data:[] as any[]};return <><div className="page-head"><div><div className="eyebrow">Rights & registry</div><h1 className="page-title">Index certificates</h1><p className="page-copy">Certificates are generated when an accepted Asset is assigned to an Index. They show the reserved Asset, diversified Index, equal Index position, contributor split and effective Index participation.</p></div></div><div className="workspace-card"><div className="table-wrap"><table className="data-table"><thead><tr><th>Certificate</th><th>Creator</th><th>Asset Index %</th><th>Effective %</th><th>Status</th><th>Document</th></tr></thead><tbody>{(data||[]).map((c:any)=><tr key={c.id}><td><strong>{c.certificate_code}</strong></td><td>{c.creator_name}</td><td>{Number(c.asset_index_percentage||0).toFixed(2)}%</td><td>{Number(c.effective_index_percentage||c.participation_percentage||0).toFixed(4)}%</td><td>{c.status}</td><td><a className="button secondary" href={`/api/certificates/${c.id}/pdf`} target="_blank">Open PDF</a></td></tr>)}</tbody></table></div></div></>}
