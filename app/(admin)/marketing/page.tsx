'use client';import{useEffect,useState}from'react';import LiveModuleWorkspace from'@/components/LiveModuleWorkspace';import{liveModules}from'@/lib/live-modules';
export default function Page(){const[files,setFiles]=useState<any[]>([]),[error,setError]=useState('');useEffect(()=>{fetch('/api/marketing/approved-files',{cache:'no-store'}).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setFiles(j.files||[])}).catch(e=>setError(e.message))},[]);return <><LiveModuleWorkspace config={liveModules.marketing}/><div className="workspace-card" style={{marginTop:24}}><div className="card-head"><div><div className="card-title">Approved Asset source library</div><p>Marketing can securely access approved Plekxa Asset files without copying or moving the authoritative master. Download links expire automatically.</p></div></div>{error&&<div className="integration-banner pending"><span>{error}</span></div>}<div className="table-wrap"><table className="data-table"><thead><tr><th>Asset</th><th>File</th><th>Category</th><th>Size</th><th>Approved source</th></tr></thead><tbody>{files.length?files.map(f=><tr key={f.id}><td><strong>{f.asset_title}</strong><small className="table-sub">{f.asset_code}</small></td><td>{f.file_name}</td><td>{f.file_category||'file'}</td><td>{f.size_bytes?`${(Number(f.size_bytes)/1024/1024).toFixed(1)} MB`:'—'}</td><td>{f.download_url?<a className="button secondary" href={f.download_url} target="_blank" rel="noreferrer">Open / download</a>:'Unavailable'}</td></tr>):<tr><td colSpan={5}>No files from approved Assets are available yet.</td></tr>}</tbody></table></div></div></>}
