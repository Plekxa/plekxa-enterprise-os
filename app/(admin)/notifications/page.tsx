'use client';import{useEffect,useState}from'react';type N=Record<string,any>;const nice=(v:any)=>String(v||'').replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase());
export default function Page(){const[items,setItems]=useState<N[]>([]),[error,setError]=useState(''),[audience,setAudience]=useState('all'),[selected,setSelected]=useState<N|null>(null);const load=()=>fetch(`/api/admin/notifications?audience=${audience}`,{cache:'no-store'}).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setItems(j.notifications||[])}).catch(e=>setError(e.message));useEffect(()=>{void load()},[audience]);return <><div className="page-head"><div><div className="eyebrow">Communications</div><h1 className="page-title">Notifications</h1><p className="page-copy">Operational notification history across Enterprise and Creator Studio, including recipient, event, destination and read state.</p></div><div className="head-actions"><select value={audience} onChange={e=>setAudience(e.target.value)}><option value="all">All audiences</option><option value="creator">Creators</option><option value="enterprise">Enterprise</option></select><button className="button secondary" onClick={()=>void load()}>Refresh</button></div></div>{error&&<div className="integration-banner pending"><span>{error}</span></div>}<div className="workspace-card"><div className="table-wrap"><table className="data-table"><thead><tr><th>Notification</th><th>Recipient</th><th>Audience</th><th>Event</th><th>Created</th><th>Status</th></tr></thead><tbody>{items.length?items.map(n=><tr className="click-row" key={n.id} onClick={()=>setSelected(n)}><td><strong>{n.title}</strong><small className="table-sub">{n.message}</small></td><td>{n.recipient_name||n.recipient_email||n.recipient_id}</td><td>{nice(n.audience||'creator')}</td><td>{nice(n.type)}</td><td>{new Date(n.created_at).toLocaleString('en-GB')}</td><td><span className={`status ${n.is_read?'good':'warn'}`}>{n.is_read?'Read':'Unread'}</span></td></tr>):<tr><td colSpan={6}>No notifications found.</td></tr>}</tbody></table></div></div>{selected&&<div className="drawer-backdrop" onMouseDown={()=>setSelected(null)}><aside className="record-drawer" onMouseDown={e=>e.stopPropagation()}><header className="drawer-head"><div><div className="eyebrow">Notification event</div><h2>{selected.title}</h2></div><button className="icon-button" onClick={()=>setSelected(null)}>×</button></header><div className="drawer-body"><div className="detail"><span>Message</span><strong>{selected.message}</strong></div><div className="detail"><span>Recipient</span><strong>{selected.recipient_name||'—'} · {selected.recipient_email||selected.recipient_id}</strong></div><div className="detail"><span>Event</span><strong>{nice(selected.type)}</strong></div><div className="detail"><span>Related record</span><strong>{nice(selected.entity_type)} {selected.entity_id||''}</strong></div><div className="detail"><span>Destination</span><strong>{selected.action_url||'—'}</strong></div><div className="detail"><span>Created</span><strong>{new Date(selected.created_at).toLocaleString('en-GB')}</strong></div><div className="detail"><span>Read</span><strong>{selected.is_read?(selected.read_at?new Date(selected.read_at).toLocaleString('en-GB'):'Yes'):'No'}</strong></div>{selected.metadata&&Object.keys(selected.metadata).length?<div className="detail"><span>Event metadata</span><strong style={{overflowWrap:'anywhere'}}>{JSON.stringify(selected.metadata)}</strong></div>:null}</div></aside></div>}</>}
