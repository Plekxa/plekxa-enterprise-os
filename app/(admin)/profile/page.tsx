'use client';
import {useEffect,useState} from 'react';
type Me={email?:string;full_name:string;role_name:string;department?:string;job_title?:string;status?:string;last_sign_in_at?:string};
export default function ProfilePage(){const[me,setMe]=useState<Me|null>(null);const[error,setError]=useState('');useEffect(()=>{fetch('/api/admin/me').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setMe(j.user)}).catch(e=>setError(e.message))},[]);return <><div className="page-head"><div><div className="eyebrow">Account</div><h1 className="page-title">My profile</h1><p className="page-copy">Your authenticated staff identity, role and account details.</p></div></div><div className="workspace-card profile-page-card">{error&&<div className="inline-warning">{error}</div>}{!me&&!error&&<p>Loading profile…</p>}{me&&<><div className="creator-profile-head"><div className="creator-avatar large">{me.full_name.split(' ').map(x=>x[0]).slice(0,2).join('').toUpperCase()}</div><div><h2>{me.full_name}</h2><p>{me.job_title||'Plekxa staff member'}</p><span className="status good">{me.status}</span></div></div><div className="detail-grid"><div className="detail"><span>Email</span><strong>{me.email}</strong></div><div className="detail"><span>Access role</span><strong>{me.role_name}</strong></div><div className="detail"><span>Department</span><strong>{me.department||'Not assigned'}</strong></div><div className="detail"><span>Last sign-in</span><strong>{me.last_sign_in_at?new Date(me.last_sign_in_at).toLocaleString():'Not available'}</strong></div></div></>}</div></>}
