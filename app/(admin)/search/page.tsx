'use client';import{useEffect,useState}from'react';import{useSearchParams,useRouter}from'next/navigation';type R={type:string;title:string;subtitle:string;href:string};export default function Page(){const p=useSearchParams(),router=useRouter(),q=p.get('q')||'';const[items,setItems]=useState<R[]>([]);const[error,setError]=useState('');useEffect(()=>{fetch(`/api/admin/search?q=${encodeURIComponent(q)}`,{cache:'no-store'}).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setItems(j.results||[])}).catch(e=>setError(e.message))},[q]);return <><div className="page-head"><div><div className="eyebrow">Global search</div><h1 className="page-title">Search results</h1><p className="page-copy">Live database results for “{q}”.</p></div></div>{error&&<div className="integration-banner pending"><div><strong>Database error</strong><span>{error}</span></div></div>}<div className="workspace-card search-results">{items.map((r,i)=><button key={`${r.type}-${i}`} onClick={()=>router.push(r.href)}><span className="status">{r.type}</span><div><strong>{r.title}</strong><small>{r.subtitle}</small></div></button>)}{q&&!items.length&&!error&&<p>No matching records were found.</p>}</div></>}
