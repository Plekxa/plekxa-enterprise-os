import {NextResponse} from 'next/server';
import {createClient} from '@supabase/supabase-js';
function db(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;return u&&k?createClient(u,k,{auth:{persistSession:false,autoRefreshToken:false}}):null}
export async function GET(){const s=db();if(!s)return NextResponse.json({error:'Supabase service credentials are not configured.'},{status:503});const{data,error}=await s.from('access_roles').select('*').order('is_master',{ascending:false}).order('name');return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({roles:data??[]})}
export async function POST(req:Request){const s=db();if(!s)return NextResponse.json({error:'Supabase service credentials are not configured.'},{status:503});const b=await req.json();if(!String(b.name||'').trim())return NextResponse.json({error:'Role name is required.'},{status:400});const{data,error}=await s.from('access_roles').insert({name:String(b.name).trim(),description:b.description||'',permissions:Array.isArray(b.permissions)?b.permissions:[],is_system:false,is_master:false}).select('*').single();return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({role:data})}
export async function PATCH(req:Request){const s=db();if(!s)return NextResponse.json({error:'Supabase service credentials are not configured.'},{status:503});const b=await req.json();if(!b.id)return NextResponse.json({error:'Role ID is required.'},{status:400});const{data,error}=await s.from('access_roles').update({name:b.name,description:b.description||'',permissions:Array.isArray(b.permissions)?b.permissions:[]}).eq('id',b.id).eq('is_master',false).select('*').single();return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({role:data})}
