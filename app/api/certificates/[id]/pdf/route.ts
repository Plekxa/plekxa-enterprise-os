import {NextResponse} from 'next/server';import {createClient} from '@supabase/supabase-js';import {buildCertificatePdf} from '@/lib/certificate-delivery';
function db(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!u||!k)throw new Error('Supabase is not configured');return createClient(u,k,{auth:{persistSession:false}})}
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,s=db();const {data:c,error}=await s.from('index_certificates').select('*').eq('id',id).single();if(error)throw error;const {bytes}=await buildCertificatePdf(s,c);return new NextResponse(bytes,{headers:{'Content-Type':'application/pdf','Content-Disposition':`inline; filename="${c.certificate_code}.pdf"`}})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Certificate unavailable'},{status:500})}}
