import {NextResponse} from 'next/server';import {createClient} from '@supabase/supabase-js';
export const dynamic='force-dynamic';function db(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!u||!k)throw new Error('Database not configured');return createClient(u,k,{auth:{persistSession:false}})}
export async function GET(){try{const s=db();const {data:indexes,error}=await s.from('plekxa_indexes').select('*').order('created_at',{ascending:false});if(error)throw error;const ids=(indexes||[]).map(x=>x.id);const {data:assets,error:ae}=ids.length?await s.from('asset_registry').select('id,title,internal_identifier,index_id,role,index_participation_percentage,status').in('index_id',ids).order('index_assigned_at'):{data:[],error:null};if(ae)throw ae;return NextResponse.json({indexes:(indexes||[]).map(i=>{const a=(assets||[]).filter(x=>x.index_id===i.id);return {...i,assets:a,counts:{flagship:a.filter(x=>x.role==='flagship').length,supporting:a.filter(x=>x.role==='supporting').length,niche:a.filter(x=>x.role==='niche').length}}})})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}
