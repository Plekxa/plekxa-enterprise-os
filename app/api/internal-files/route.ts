import {NextResponse} from 'next/server';import {createClient} from '@supabase/supabase-js';import {PutObjectCommand,GetObjectCommand,CreateMultipartUploadCommand,UploadPartCommand,CompleteMultipartUploadCommand,AbortMultipartUploadCommand} from '@aws-sdk/client-s3';import {getSignedUrl} from '@aws-sdk/s3-request-presigner';import {r2Client,safeFileName} from '@/lib/r2';
const BUCKET=process.env.CLOUDFLARE_R2_INTERNAL_BUCKET||'plekxa-internal';function db(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!u||!k)throw new Error('Supabase service credentials are not configured.');return createClient(u,k,{auth:{persistSession:false}})}
export async function GET(){try{const s=db();const {data,error}=await s.from('internal_files').select('*').is('archived_at',null).order('created_at',{ascending:false});if(error)throw error;const c=r2Client();return NextResponse.json({files:await Promise.all((data||[]).map(async f=>({...f,download_url:await getSignedUrl(c,new GetObjectCommand({Bucket:f.r2_bucket||BUCKET,Key:f.r2_key}),{expiresIn:900})})))})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}
export async function POST(r:Request){try{const s=db(),b=await r.json(),action=String(b.action||'presign'),c=r2Client();if(action==='presign'||action==='multipart-create'){const department=safeFileName(String(b.department||'General')),name=safeFileName(String(b.relative_path||b.file_name||'file')),key=`${department}/${new Date().getFullYear()}/${crypto.randomUUID()}-${name}`,type=b.mime_type||'application/octet-stream';if(action==='presign'&&Number(b.size_bytes)<=5*1024**3)return NextResponse.json({mode:'single',upload_url:await getSignedUrl(c,new PutObjectCommand({Bucket:BUCKET,Key:key,ContentType:type}),{expiresIn:3600}),key,bucket:BUCKET});const x=await c.send(new CreateMultipartUploadCommand({Bucket:BUCKET,Key:key,ContentType:type}));return NextResponse.json({mode:'multipart',upload_id:x.UploadId,key,bucket:BUCKET,part_size:100*1024*1024})}if(action==='multipart-part')return NextResponse.json({upload_url:await getSignedUrl(c,new UploadPartCommand({Bucket:BUCKET,Key:b.key,UploadId:b.upload_id,PartNumber:Number(b.part_number)}),{expiresIn:3600})});if(action==='multipart-complete'){await c.send(new CompleteMultipartUploadCommand({Bucket:BUCKET,Key:b.key,UploadId:b.upload_id,MultipartUpload:{Parts:(b.parts||[]).map((x:any)=>({ETag:x.etag,PartNumber:Number(x.part_number)}))}}));return NextResponse.json({ok:true})}if(action==='multipart-abort'){await c.send(new AbortMultipartUploadCommand({Bucket:BUCKET,Key:b.key,UploadId:b.upload_id}));return NextResponse.json({ok:true})}if(action==='confirm'){const {data,error}=await s.from('internal_files').insert({department:b.department||'General',category:b.category||null,title:b.title||b.file_name,r2_bucket:BUCKET,r2_key:b.key,file_name:b.file_name,content_type:b.mime_type,size_bytes:b.size_bytes}).select('*').single();if(error)throw error;return NextResponse.json({file:data})}return NextResponse.json({error:'Unknown action'},{status:400})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}