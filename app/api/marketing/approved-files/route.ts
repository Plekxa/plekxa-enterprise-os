import {NextResponse} from 'next/server';import {createClient} from '@supabase/supabase-js';import {GetObjectCommand} from '@aws-sdk/client-s3';import {getSignedUrl} from '@aws-sdk/s3-request-presigner';import {r2Client,R2_BUCKET} from '@/lib/r2';
export const dynamic='force-dynamic';
export async function GET(){try{const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!u||!k)throw new Error('Database not configured');const s=createClient(u,k,{auth:{persistSession:false}});const{data:assets,error:ae}=await s.from('asset_registry').select('id,title,internal_identifier,status').in('status',['approved','active','Approved','Active']).order('created_at',{ascending:false});if(ae)throw ae;const ids=(assets||[]).map(a=>a.id);if(!ids.length)return NextResponse.json({files:[]});const{data:files,error}=await s.from('asset_files').select('*').in('asset_id',ids).order('created_at',{ascending:false});if(error)throw error;const c=r2Client();const out=await Promise.all((files||[]).map(async f=>{const a=(assets||[]).find(x=>x.id===f.asset_id);const key=f.r2_key||f.storage_path;let download_url:string|null=null;if(key)download_url=await getSignedUrl(c,new GetObjectCommand({Bucket:f.r2_bucket||R2_BUCKET,Key:key}),{expiresIn:900});return{...f,asset_title:a?.title||'Asset',asset_code:a?.internal_identifier||'',asset_status:a?.status||'',download_url}}));return NextResponse.json({files:out})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}
