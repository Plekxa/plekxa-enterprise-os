'use client';import Image from 'next/image';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const s=createClient();if(!s){setMsg('Add the Supabase environment variables to enable sign-in.');return}const{error}=await s.auth.signInWithPassword({email,password});if(error)setMsg(error.message);else location.href='/dashboard'}return <div className="login-page"><section className="login-brand"><Image src="/brand/plekxa-logo.png" alt="Plekxa" width={160} height={40}/><div><h1>Run the company from one place.</h1><p>Manage creators, projects, rights, content, audiences, finance and the corporate website through one secure operations console.</p></div><small>Restricted to authorised Plekxa personnel.</small></section><section className="login-form-wrap"><form className="login-form" onSubmit={submit}><div className="eyebrow">Plekxa Admin</div><h2>Welcome back</h2><p className="page-copy">Sign in with your authorised staff account.</p>{msg&&<div className="notice">{msg}</div>}<div className="field"><label>Email address</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="button primary" style={{width:'100%',justifyContent:'center'}}>Sign in</button></form></section></div>}
