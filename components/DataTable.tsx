export type Row=Record<string,string|number|undefined>;
export default function DataTable({columns,rows}:{columns:{key:string,label:string}[],rows:Row[]}){return <div className="card table-wrap"><table className="data-table"><thead><tr>{columns.map(c=><th key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.length?rows.map((r,i)=><tr key={i}>{columns.map(c=><td key={c.key}>{c.key==='status'?<span className={`status ${String(r[c.key]).match(/active|approved|published|resolved|paid/i)?'good':String(r[c.key]).match(/pending|review|draft|open/i)?'warn':''}`}>{r[c.key]}</span>:r[c.key]}</td>)}</tr>):<tr><td colSpan={columns.length}><div className="empty">No records yet. Connect Supabase and add your first record.</div></td></tr>}</tbody></table></div>}
