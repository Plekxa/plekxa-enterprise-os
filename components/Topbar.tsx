'use client';
import {Bell,Search} from '@/components/icons';
import {useRouter} from 'next/navigation';
import {useState} from 'react';
export default function Topbar(){const [q,setQ]=useState('');const [open,setOpen]=useState(false);const router=useRouter();function submit(e:React.FormEvent){e.preventDefault();if(q.trim())router.push(`/projects?search=${encodeURIComponent(q.trim())}`)}return <header className="topbar"><form className="search" onSubmit={submit}><Search size={15}/><input suppressHydrationWarning value={q} onChange={e=>setQ(e.target.value)} placeholder="Search projects, creators, content…"/></form><div className="user-chip"><button suppressHydrationWarning className="icon-button notification-button" onClick={()=>setOpen(!open)} aria-label="Notifications"><Bell size={18}/><i/></button><button suppressHydrationWarning className="profile-button" onClick={()=>router.push('/people')}><div className="avatar">AI</div><span>Anthony Ighomena</span></button>{open&&<div className="notification-popover"><strong>Notifications</strong><p>3 applications are awaiting review.</p><p>A newsroom draft is ready for approval.</p><button suppressHydrationWarning className="button secondary" onClick={()=>router.push('/notifications')}>View all</button></div>}</div></header>}
